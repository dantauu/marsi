import { Link } from "@tanstack/react-router"

const Search = () => {
  return (
    <div>
      <Link to="/profile">
        <button>Go to profile</button>
      </Link>
    </div>
  )
}

export default Search
