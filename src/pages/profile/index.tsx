import { Link } from "@tanstack/react-router"

const Profile = () => {
  return (
    <div>
      <Link to="/search">
        <button>Go to search</button>
      </Link>
    </div>
  )
}

export default Profile
